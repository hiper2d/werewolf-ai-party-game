// External community / social links, referenced from a single place so the
// invite only ever needs to be swapped here.
export const DISCORD_URL = 'https://discord.gg/PVY9dpU8X5';

// Canonical public origin, no trailing slash. Used for page metadata and for the
// absolute links that /llms.txt has to emit.
export const SITE_URL = 'https://aiwerewolf.net';

// Where copyright / content concerns land. Public on the Terms page, so it is
// deliberately a single address rather than scattered mailto links.
export const CONTACT_EMAIL = 'hiper2d@gmail.com';
